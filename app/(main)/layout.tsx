const MainLayout = async (
    {
        children
    }:
        {
            children: React.ReactNode;
        }) => {
    return (
        <div>
            {children}
        </div>

    )
};

export default MainLayout;